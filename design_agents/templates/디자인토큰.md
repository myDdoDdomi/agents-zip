# 디자인 토큰: <도메인/테마명>

> 작성: `design-system-architect` · 출처(source of truth): (Figma 변수 / 코드 / 이 문서) · 최종수정: YYYY-MM-DD

## 0. 원칙
- 계층: **원시(primitive) → 의미(semantic) → 컴포넌트(component)**
- 기존 Figma 변수·코드 토큰을 **재사용/확장**(중복 신설 금지)

## 1. 색 (Color)
| 토큰 | 값(HEX) | 계층 | 용도 | 대비 검증 |
|---|---|---|---|---|
| `color.brand.600` | `#______` | primitive | | — |
| `color.action.primary` | → `color.brand.600` | semantic | 주요 버튼 | 본문 위 ≥4.5:1 ✅/❌ |
| `button.bg.default` | → `color.action.primary` | component | 버튼 배경 | |

## 2. 타이포그래피 (Typography)
| 토큰 | 폰트 | 크기 | 행간 | 굵기 | 용도 |
|---|---|---|---|---|---|
| `font.heading.lg` | | | | | |

## 3. 스페이싱 · 반경 · 그림자
| 토큰 | 값 | 용도 |
|---|---|---|
| `space.4` | `16px` | |
| `radius.md` | `8px` | |
| `shadow.1` | | |

## 4. Figma 동기화 (drift)
| 토큰 | 이 문서 | Figma 변수 | 일치? | 동기화 방향 |
|---|---|---|---|---|
| | | | ✅/❌ | |

> Figma에 토큰 push(쓰기)는 `/figma-sync` + 사람 승인.

## 5. 미확정
- `(확인 필요: …)`
