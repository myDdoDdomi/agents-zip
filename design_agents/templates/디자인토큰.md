# 디자인 토큰: <도메인/테마명>

> 작성: `design-system-architect` · 출처(source of truth): (Figma 변수 / 코드 / 이 문서) · 최종수정: YYYY-MM-DD

## 0. 원칙
- 계층: **원시(primitive) → 의미(semantic) → 컴포넌트(component)**
- 기존 Figma 변수·코드 토큰을 **재사용/확장**(중복 신설 금지)

## 1. 색 (Color)
> 시맨틱 색 토큰은 **라이트/다크 쌍**으로 정의한다(원시 토큰은 모드 무관 고정, 모드 분기는 시맨틱에서). 대비는 라이트·다크 각각.
> (주의: 기존 단일값 토큰 문서와 구조가 다를 수 있음 → **신규 작성부터 적용**, 기존 문서는 점진 마이그레이션.)

| 토큰 | 값(라이트) | 값(다크) | 계층 | 용도 | 대비(라이트) | 대비(다크) |
|---|---|---|---|---|---|---|
| `color.brand.600` | `#______` | `#______` | primitive | | — | — |
| `color.action.primary` | → `color.brand.600` | → `color.brand.500` | semantic | 주요 버튼 | ≥4.5:1 ✅/❌ | ≥4.5:1 ✅/❌ |
| `button.bg.default` | → `color.action.primary` | → `color.action.primary` | component | 버튼 배경 | | |

## 2. 타이포그래피 (Typography)
| 토큰 | 폰트 | 크기 | 행간 | 굵기 | 용도 |
|---|---|---|---|---|---|
| `font.heading.lg` | | | | | |

## 3. 스페이싱 · 반경 · 그림자
| 토큰 | 값 | 용도 |
|---|---|---|
| `space.4` | `16px` | |
| `radius.md` | `8px` | |
| `shadow.1` | | |

## 4. Figma 동기화 (drift)
| 토큰 | 이 문서 | Figma 변수 | 일치? | 동기화 방향 |
|---|---|---|---|---|
| | | | ✅/❌ | |

> Figma에 토큰 push(쓰기)는 `/figma-sync` + 사람 승인.

## 4-1. Tailwind 정합 (Figma Variable ↔ Tailwind config 1:1)
> 코드가 Tailwind를 쓰면, 시맨틱 토큰 이름이 `tailwind.config` `theme.extend` 키와 1:1 대응되는지 점검한다.

| Figma Variable | Tailwind config 키 | 일치 여부 |
|---|---|---|
| `color/action/primary` | `extend.colors.action.primary` | ✅/❌ |
| `radius/md` | `extend.borderRadius.md` | ✅/❌ |

> 다크모드: Tailwind 다크 전략(`class`/`media` + CSS 변수)에 모드별 값이 어떻게 노출되는지 비고에 적는다. `(확인 필요: 실제 config 키는 소스 기준)`

## 5. 미확정
- `(확인 필요: …)`
