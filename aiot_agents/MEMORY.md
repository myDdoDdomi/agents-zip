# aiot-agents — 팀 메모리 (MEMORY.md)

> **이 파일은 팀의 "학습 누적층"이다.** 그룹장(CLAUDE.md)이 `@./MEMORY.md`로 매 세션 자동 로드한다.
> CLAUDE.md(사람이 쓰는 안정적 규칙·라우팅)와 역할이 다르다 — 여기엔 **작업하며 검증된 함정·우회법·환경 매핑·반복 교정**처럼 *바뀌고 쌓이는 운영 지식*을 적는다.
>
> **유지 규칙:** ① 200줄 이하 유지(초과분은 `memory/<주제>.md`로 분리—온디맨드). ② 한 항목=한 사실(가능하면 날짜·근거). ③ CLAUDE.md와 모순 금지. ④ 틀린 학습은 삭제. ⑤ 비밀값·디바이스 키·인증서 금지.
> **언제 적나:** 같은 실수 2회 · 환경/도구 특이사항(SoC 핀맵·RTOS 빌드 함정 등) · 매 세션 다시 설명하게 되는 것 · `/feedback-agents` 회고에서 즉시 재사용 가능한 학습.

## 1. 검증된 함정·우회법 (Gotchas & Workarounds)
- (아직 누적된 학습 없음 — 작업하며 채움: SoC 빌드/플래시 함정·프로토콜 스택 버전 호환·양자화 정확도 손실 등)

## 2. 환경·프로젝트 매핑 (Env & Project Mapping)
- 도메인 범위는 **범용**(IIoT·스마트홈·웨어러블·로보틱스·스마트시티). 각 에이전트가 요청에서 도메인을 식별해 내부 분기. (2026-06-07, 설계 확정)
- MCP: **Context7(read-only)** 로 SoC/RTOS/SDK/프로토콜 최신 문서 조회. **GitHub MCP는 선택**(레퍼런스 레포 읽기 근거, 쓰기 금지).
- 구현 경계: `firmware-embedded-engineer`·`iot-cloud-data-engineer`는 **참조 구현·프로토타입까지** 한다. 양산 구현·정식 코드리뷰·펜테스트는 `dev_agents`로 핸드오프/조립. (2026-06-07, 설계 확정)

## 3. 반복 교정·선호 (Recurring Corrections / Preferences)
- 물리/하드웨어 수치(전력·대역폭·레이턴시·BOM·온도)는 **사용자 확인값**만 단정. 모르면 `(확인 필요)`. 임의 수치로 설계 정당화 금지. (2026-06-07, 설계 원칙)
- 새 디바이스/연결/클라우드 경계가 생기면 **STRIDE 위협모델을 설계 단계에서** 적용(보안을 사후에 붙이지 않음). (2026-06-07, 설계 원칙)

## 4. 도구·명령 메모 (Tools & Commands)
- (아직 누적된 학습 없음 — 작업하며 채움: 크로스컴파일 툴체인·플래시 도구·클라우드 IoT CLI 등)

## 5. 미해결·주의 (Open Issues / Cautions)
- [워치리스트] MCP 2026-07-28 RC(stateless·Tasks 등) 정식 릴리스 시 `.mcp.json` 갱신 필요 — 본부 `/update-agents` diff로 재점검. (2026-06-07, 근거: 본부 MEMORY)
